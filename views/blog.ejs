<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head.ejs') %>
    <title>Blog Page</title>
</head>
<body>
    <%- include('./partials/nav.ejs') %>
    <div class="container mt-3">
        <h1><%= blog.title %></h1>
        <img src="<%= blog.createdBy.profileImageUrl %>" alt="" width="44px"> <%= blog.createdBy.fullName%>
        <hr>
        <img src="<%= blog.coverImageURL %>" alt="" width="700vw">
        <pre class="mt-3"><%= blog.body %></pre>
    </div>
    <div class="container mt-3 mb-3">
        <h2>Comments (<%= comments.length %>)</h2>
        <% if (locals.user) { %>
            <form action="/blog/comment/<%= blog._id %>" method="post">
                <div class="mb-3">
                    <input type="text" class="form-control" name="content" placeholder="What are your thoughts?">
                    
                </div>
                <button type="submit" class="btn btn-sm btn-secondary">Submit</button>
            </form>
        <% } %>
        <% comments.forEach(comment => { %>
         <div class="mt-3">
            <img src="<%= comment.createdBy.profileImageUrl %>" alt="img" width="44px" class="rounded-circle"> <%= comment.createdBy.fullName%>
            <pre><%= comment.content %></pre>
         </div>
        <% }) %>
        
    </div>
    <%- include('./partials/script.ejs') %>
</body>
</html>